# The configuration this binary reads. Copy to config.toml, which is
# gitignored; this example is tracked and is the documentation.
#
# On the server, config.toml is written by `make secrets-install` from
# secrets.env, so the secret-bearing sections below arrive from there rather
# than being edited by hand over SSH.

[server]
# Two listeners, and they must differ. Which socket accepted a request is how
# the embedded form and the management app are told apart: Apache's
# ProxyPreserveHost cannot be set in .htaccess, so a proxied request arrives
# with Host: 127.0.0.1:<port> whatever the visitor typed. Each konsoleH
# document root has its own .htaccess pointing at one of these ports.
#
#   public_html/f.schoenstatt.link/public/     -> embed_addr
#   public_html/forms.schoenstatt.link/public/ -> admin_addr
#
# Two, not three: the Stripe webhook shares the embed listener, at
# /stripe/webhook, mounted outside that surface's origin gate. See muxer.Embed.
embed_addr = "127.0.0.1:8410"
admin_addr = "127.0.0.1:8411"

# This service's own admin origin. It is the address that goes into a sign-in
# email, and it is configured rather than taken from the request on purpose: a
# link built from a Host header is one an attacker can aim at their own host by
# sending a single request, and whoever receives the mail cannot tell.
admin_base_url = "https://forms.schoenstatt.link"

# How long in-flight requests get after a stop signal. Long enough for a
# submission to finish writing, short enough that a restart is not a wait.
shutdown_grace = "15s"

[embed]
# Who may frame an embedded form, until each form carries its own list.
#
# This becomes the CSP frame-ancestors directive. An empty list means nothing
# may frame anything -- the policy fails closed on purpose, because a CSP that
# merely omits frame-ancestors is frameable by the entire web.
#
# A list rather than one value because frame-ancestors checks *every* ancestor,
# not just the immediate parent, and Squarespace nests pages inside editor
# chrome. Each entry must be https, with no path: https://example.org or
# https://*.example.org.
allowed_origins = ["https://schoenstatt-austin.us"]

# Signs the submission grant that every form page hands out and every
# submission hands back: an HMAC over the form's id, its version, a nonce and
# an expiry. It is what pins the price list into a public POST.
#
# Required. There is no default and no degraded mode, because a form page
# cannot be rendered without a grant to put in it.
#
# Generate one with:  openssl rand -base64 48
#
# Rotating it invalidates every form page currently open in a browser, which
# costs a reload and no data -- so rotate it freely, though not while tickets
# are selling.
grant_signing_key = ""

# Whether X-Forwarded-For carries the visitor's address.
#
# False by default, and it has to be set rather than detected. In production
# this process listens on the loopback behind Apache, so the socket's own
# address is always 127.0.0.1 and the visitor's address exists only in a
# header -- and a header is exactly as trustworthy as whatever put it there.
# Trusting one with nothing in front means letting a stranger choose the
# address that goes to Stripe's fraud checks, which is worse than having none.
trust_proxy = false

[db]
# Above the document root, always. The deploy refuses to ship if this file can
# be fetched over HTTPS.
path = "dropin-forms.db"

[log]
# debug, info, warn or error.
level = "info"

# Empty means stderr, which is what a developer wants. On the server the
# supervisor redirects stderr into a file, so this can stay empty there too.
file = ""

[auth]
# One-time secret that produces a session without sending any mail.
#
# It exists because there is no CLI: reading submissions needs a session,
# signing in needs mail, and mail needs to be configured correctly -- so
# without a way in that sends nothing, a misconfigured relay is not an
# inconvenience, it is being locked out of your own service.
#
# It works exactly once. The claim is recorded in the database, so restarting
# does not restore it and writing a new secret here does not revive it. Leave
# it empty to leave the route unmounted entirely, which is the right answer
# once the service has accounts.
#
# At least 32 characters, refused at startup below that: a short one is a
# guessable route to a founding session that leaves no trace anywhere but our
# own log.
bootstrap_secret = ""

[stripe]
# Taking payment. Both empty switches payments off entirely: forms still
# validate and store their submissions, and a form that sells shows no payment
# button. Either one set requires the other, because a half-configured payment
# path is the one shape worth refusing at startup -- a key with no webhook
# secret means money collected and no order ever marked paid, and nothing
# anywhere would say so.
#
# There is no address here. The webhook is a route on the embed listener,
# /stripe/webhook, mounted outside that surface's origin gate -- so the URL for
# the Stripe dashboard is https://f.schoenstatt.link/stripe/webhook.

# The API key, sk_test_... while rehearsing and sk_live_... when selling. The
# log says which mode it is on every start, from the prefix; it never prints
# the key.
secret_key = ""

# The endpoint's signing secret, whsec_..., and a DIFFERENT credential from the
# key above. Stripe shows it when you create the webhook endpoint, and
# `stripe listen` prints its own for local work. Confusing the two gives a
# signature that never verifies, so every payment stays pending.
#
# This is what makes the webhook safe to expose. Without it there would be a
# public URL that marks orders paid.
webhook_secret = ""


[mail]
# The relay that sends sign-in links and receipts.
#
# It has to be a relay that already passes SPF and DKIM for the sending
# domain. Mail sent straight from a small host lands in spam, and for a
# sign-in link that means nobody can log in.
#
# Leave host empty to record messages instead of sending them, which is what a
# local run wants. The process says so loudly in its log rather than failing to
# start.
host = ""
port = 587
user = ""
password = ""
from = "forms@schoenstatt.link"
from_name = "Schoenstatt Austin"

# Told about every submission on every form.
#
# Three things decide who hears about a submission, and they answer different
# questions: this one is "where does this installation's post go", a form's own
# `notify` list is "who else cares about this particular form" -- the kitchen,
# for a lunch -- and the accounts holding `results` on the form are "who has
# been given the job of reading these", which is the list that stays right when
# somebody leaves.
#
# Empty is allowed and means the other two are the only ways. A submission that
# reaches nobody by any of the three is logged as a warning, because a service
# that stores orders nobody is told about is the failure this exists to
# prevent.
notify = ""
